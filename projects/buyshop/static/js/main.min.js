'use strict';

var buttons = document.querySelectorAll('button');

if (buttons) {
  for (var i = 0; i < buttons.length; i++) {
    buttons[i].addEventListener('mouseup', function () {
      this.blur();
    });
  }
}

var hamburger = document.querySelector('.header__icon-menu');
var nav = document.querySelector('.header__nav');
var btnClose = document.querySelector('.header__icon-close');
var body = document.querySelector('body');

var openMenu = function openMenu() {
  body.classList.add('fixed');
  nav.classList.add('header__nav--active');
  hamburger.classList.add('icon__menu--active');
};

var closeMenu = function closeMenu() {
  body.classList.remove('fixed');
  nav.classList.remove('header__nav--active');
  hamburger.classList.remove('icon__menu--active');
};

hamburger.addEventListener('click', function () {
  openMenu();
});
btnClose.addEventListener('click', function () {
  closeMenu();
});
nav.addEventListener('click', function (evt) {
  if (evt.target === nav) {
    closeMenu();
  }
});

if (document.querySelector('.header__cat-btn')) {
  var headerCategoryBtn = document.querySelector('.header__cat-btn');
  headerCategoryBtn.addEventListener('click', function () {
    headerCategoryBtn.classList.toggle('header__cat-btn--active');
  });
}

if (document.querySelector('.accordion-btn')) {
  var accordionBtns = document.querySelectorAll('.accordion-btn');
  accordionBtns.forEach(function (btn, index) {
    btn.addEventListener('click', function (evt) {
      evt.currentTarget.classList.toggle('accordion-btn--active');
    });
  });
}

if (document.querySelector('.header__btn-account')) {
  var account = document.querySelector('.header__btn-account');
  account.addEventListener('click', function (evt) {
    evt.currentTarget.classList.toggle('header__btn-account--active');
  });
}

if (document.querySelector('.header__btn-cart')) {
  var cart = document.querySelector('.header__btn-cart');
  cart.addEventListener('click', function (evt) {
    evt.currentTarget.classList.toggle('header__btn-cart--active');
  });
}

if (document.querySelector('.header__select-toggle')) {
  var toggle = document.querySelector('.header__select-toggle');
  toggle.addEventListener('click', function (evt) {
    evt.currentTarget.classList.toggle('header__select-toggle--active');
  });
}

if (document.querySelector('.menu-aside__link')) {
  var arrowsMenu = document.querySelectorAll('.menu-aside__link');
  arrowsMenu.forEach(function (arrow, index) {
    arrow.addEventListener('click', function (evt) {
      evt.currentTarget.classList.toggle('menu-aside__link--active');
    });
  });
}

var promoSlider = new Swiper('.promo-slider', {
  loop: true,
  pagination: {
    el: '.promo-slider__pagination',
    clickable: true
  },
  slidesPerView: 1,
  autoplay: {
    delay: 5000
  }
});
var topProductSlider = new Swiper('.top-product__slider', {
  navigation: {
    nextEl: '.top-product__btn-next',
    prevEl: '.top-product__btn-prev'
  },
  slidesPerView: 1
});
var topCategorySlider = new Swiper('.top-category-slider__inner', {
  navigation: {
    nextEl: '.top-category-slider__btn-next',
    prevEl: '.top-category-slider__btn-prev'
  },
  slidesPerView: 4,
  spaceBetween: 20,
  loop: true,
  breakpoints: {
    320: {
      slidesPerView: 2
    },
    576: {
      slidesPerView: 3
    },
    768: {
      slidesPerView: 3
    },
    992: {
      slidesPerView: 3
    },
    1200: {
      slidesPerView: 4
    }
  }
});
var reviewsSlider = new Swiper('.reviews-aside__slider', {
  pagination: {
    el: '.reviews-aside__pagination',
    clickable: true
  },
  slidesPerView: 1
});
var timerSlider = new Swiper('.timer__slider', {
  slidesPerView: 1,
  // loop: true,
  autoplay: {
    delay: 3000
  },
  breakpoints: {
    320: {
      slidesPerView: 1
    },
    576: {
      slidesPerView: 2
    },
    768: {
      slidesPerView: 1
    }
  }
});
var bestSellingSlider = new Swiper('.best-selling__slider', {
  navigation: {
    nextEl: '.best-selling__btn-next',
    prevEl: '.best-selling__btn-prev'
  },
  slidesPerView: 1
});
var newArrivalSlider = new Swiper('.new-arrival__slider', {
  navigation: {
    nextEl: '.new-arrival__btn-next',
    prevEl: '.new-arrival__btn-prev'
  },
  spaceBetween: 20,
  breakpoints: {
    320: {
      slidesPerView: 1
    },
    576: {
      slidesPerView: 2
    },
    768: {
      slidesPerView: 2
    },
    992: {
      slidesPerView: 2
    },
    1200: {
      slidesPerView: 3
    }
  }
});
var newsSlider = new Swiper('.news__slider', {
  navigation: {
    nextEl: '.news__btn-next',
    prevEl: '.news__btn-prev'
  },
  spaceBetween: 20,
  loop: true,
  breakpoints: {
    320: {
      slidesPerView: 1
    },
    576: {
      slidesPerView: 2
    },
    768: {
      slidesPerView: 2
    },
    992: {
      slidesPerView: 3
    },
    1200: {
      slidesPerView: 2
    }
  }
});
var vendorsSlider = new Swiper('.vendors__slider', {
  slidesPerView: 7,
  loop: true,
  autoplay: {
    delay: 2000
  },
  breakpoints: {
    320: {
      slidesPerView: 2
    },
    576: {
      slidesPerView: 3
    },
    768: {
      slidesPerView: 4
    },
    992: {
      slidesPerView: 5
    },
    1200: {
      slidesPerView: 7
    }
  }
});
var miniSlider = new Swiper('.product__mini-slider', {
  navigation: {
    nextEl: '.product__mini-btn-next',
    prevEl: '.product__mini-btn-prev'
  },
  slidesPerView: 4,
  breakpoints: {
    320: {
      slidesPerView: 3
    },
    426: {
      slidesPerView: 4
    },
    576: {
      slidesPerView: 5
    },
    768: {
      slidesPerView: 3
    },
    992: {
      slidesPerView: 3
    },
    1200: {
      slidesPerView: 4
    }
  }
});
var productSlider = new Swiper('.product__slider', {
  slidesPerView: 1,
  navigation: {
    nextEl: '.product__mini-btn-next',
    prevEl: '.product__mini-btn-prev'
  },
  zoom: {
    maxRatio: 5,
    minRatio: 1
  },
  thumbs: {
    swiper: miniSlider
  }
});
var likeProductSlider = new Swiper('.like-product__slider', {
  navigation: {
    nextEl: '.like-product__btn-next',
    prevEl: '.like-product__btn-prev'
  },
  spaceBetween: 30,
  loop: true,
  breakpoints: {
    320: {
      slidesPerView: 1
    },
    576: {
      slidesPerView: 3
    },
    768: {
      slidesPerView: 3
    },
    992: {
      slidesPerView: 4
    },
    1200: {
      slidesPerView: 4
    }
  }
});
var relatedProductSlider = new Swiper('.related-product__slider', {
  navigation: {
    nextEl: '.related-product__btn-next',
    prevEl: '.related-product__btn-prev'
  },
  spaceBetween: 30,
  loop: true,
  breakpoints: {
    320: {
      slidesPerView: 1
    },
    576: {
      slidesPerView: 3
    },
    768: {
      slidesPerView: 3
    },
    992: {
      slidesPerView: 4
    },
    1200: {
      slidesPerView: 4
    }
  }
});

if (document.querySelector('.tabs__btn')) {
  var tabs = document.querySelectorAll('.tabs__btn');
  var tabsContent = document.querySelectorAll('.tabs__content');
  tabs.forEach(function (tab, index) {
    tab.addEventListener('click', function (evt) {
      tabs.forEach(function (tab) {
        tab.classList.remove('tabs__btn--active');
      });
      evt.currentTarget.classList.add('tabs__btn--active');
      tabsContent.forEach(function (tabContent) {
        tabContent.classList.remove('tabs__content--active');
      });
      tabsContent[index].classList.add('tabs__content--active');
    });
  });
}

if (document.querySelector('.tabs-category__btn')) {
  var _tabs = document.querySelectorAll('.tabs-category__btn');

  var _tabsContent = document.querySelectorAll('.tabs-category__content');

  _tabs.forEach(function (tab, index) {
    tab.addEventListener('click', function (evt) {
      _tabs.forEach(function (tab) {
        tab.classList.remove('tabs-category__btn--active');
      });

      evt.currentTarget.classList.add('tabs-category__btn--active');

      _tabsContent.forEach(function (tabContent) {
        tabContent.classList.remove('tabs-category__content--active');
      });

      _tabsContent[index].classList.add('tabs-category__content--active');
    });
  });
}

if (document.querySelector('.tabs-product__btn')) {
  var _tabs2 = document.querySelectorAll('.tabs-product__btn');

  var _tabsContent2 = document.querySelectorAll('.tabs-product__content');

  _tabs2.forEach(function (tab, index) {
    tab.addEventListener('click', function (evt) {
      _tabs2.forEach(function (tab) {
        tab.classList.remove('tabs-product__btn--active');
      });

      evt.currentTarget.classList.add('tabs-product__btn--active');

      _tabsContent2.forEach(function (tabContent) {
        tabContent.classList.remove('tabs-product__content--active');
      });

      _tabsContent2[index].classList.add('tabs-product__content--active');
    });
  });
}

if (document.querySelector('.filter__btn')) {
  var filterBtns = document.querySelectorAll('.filter__btn');
  filterBtns.forEach(function (btn, index) {
    btn.addEventListener('click', function (evt) {
      evt.currentTarget.classList.toggle('filter__btn--active');
    });
  });
}

if (document.querySelector('.checkout__info-link')) {
  var couponLink = document.querySelector('.checkout__info-link');
  var couponForm = document.querySelector('.checkout__coupon');
  couponLink.addEventListener('click', function (evt) {
    couponForm.classList.toggle('checkout__coupon--active');
  });
}

if (document.querySelector('.card__hover-wrap-img')) {
  var imgs = document.querySelectorAll('.card__hover-wrap-img');
  var cards = document.querySelectorAll('.card__hover');
  cards.forEach(function (card, index) {
    card.onmouseover = function () {
      imgs.forEach(function (img, index) {
        img.classList.add('animate-fadeInDown');
        img.classList.remove('animate-fadeInUp');
      });
    };

    card.onmouseout = function (e) {
      imgs.forEach(function (img, index) {
        img.classList.remove('animate-fadeInDown');
        img.classList.add('animate-fadeInUp');
      });
    };
  });
}

if (document.querySelector('.new-arrival__hover-img-wrap')) {
  var _imgs = document.querySelectorAll('.new-arrival__hover-img-wrap');

  var _cards = document.querySelectorAll('.new-arrival__card');

  _cards.forEach(function (card, index) {
    card.onmouseover = function () {
      _imgs.forEach(function (img, index) {
        img.classList.add('animate-fadeInDown');
        img.classList.remove('animate-fadeInUp');
      });
    };

    card.onmouseout = function (e) {
      _imgs.forEach(function (img, index) {
        img.classList.remove('animate-fadeInDown');
        img.classList.add('animate-fadeInUp');
      });
    };
  });
}

var getTimeRemaining = function getTimeRemaining(endtime) {
  var t = Date.parse(endtime) - Date.parse(new Date());
  var seconds = Math.floor(t / 1000 % 60);
  var minutes = Math.floor(t / 1000 / 60 % 60);
  var hours = Math.floor(t / (1000 * 60 * 60) % 24);
  var days = Math.floor(t / (1000 * 60 * 60 * 24));
  return {
    'total': t,
    'days': days,
    'hours': hours,
    'minutes': minutes,
    'seconds': seconds
  };
};

var initializeClock = function initializeClock(endtime, el) {
  var daysSpan = el.querySelector(".days");
  var hoursSpan = el.querySelector(".hours");
  var minutesSpan = el.querySelector(".minutes");
  var secondsSpan = el.querySelector(".seconds");

  var updateClock = function updateClock() {
    var t = getTimeRemaining(endtime);

    if (t.total <= 0) {
      el.classList.add("timer__countdown--hidden");
      el.nextElementSibling.classList.add("timer__deadline-message--visible");
      clearInterval(timeinterval);
      return true;
    }

    daysSpan.innerHTML = t.days;
    hoursSpan.innerHTML = ("0" + t.hours).slice(-2);
    minutesSpan.innerHTML = ("0" + t.minutes).slice(-2);
    secondsSpan.innerHTML = ("0" + t.seconds).slice(-2);
  };

  updateClock();
  var timeinterval = setInterval(updateClock, 1000);
};

var deadlines = document.querySelectorAll('[data-time]').forEach(function (el, index) {
  var timer = el.getAttribute('data-time');
  initializeClock(timer, el);
});

if (document.querySelector('.footer__btn')) {
  var footerBtns = document.querySelectorAll('.footer__btn');
  footerBtns.forEach(function (btn, index) {
    btn.addEventListener('click', function (evt) {
      evt.currentTarget.classList.toggle('footer__btn--active');
    });
  });
}

"use strict";

function DynamicAdapt(type) {
  this.type = type;
}

DynamicAdapt.prototype.init = function () {
  var _this2 = this;

  var _this = this; // массив объектов


  this.оbjects = [];
  this.daClassname = "_dynamic_adapt_"; // массив DOM-элементов

  this.nodes = document.querySelectorAll("[data-da]"); // наполнение оbjects объктами

  for (var _i = 0; _i < this.nodes.length; _i++) {
    var node = this.nodes[_i];
    var data = node.dataset.da.trim();
    var dataArray = data.split(",");
    var оbject = {};
    оbject.element = node;
    оbject.parent = node.parentNode;
    оbject.destination = document.querySelector(dataArray[0].trim());
    оbject.breakpoint = dataArray[1] ? dataArray[1].trim() : "767";
    оbject.place = dataArray[2] ? dataArray[2].trim() : "last";
    оbject.index = this.indexInParent(оbject.parent, оbject.element);
    this.оbjects.push(оbject);
  }

  this.arraySort(this.оbjects); // массив уникальных медиа-запросов

  this.mediaQueries = Array.prototype.map.call(this.оbjects, function (item) {
    return '(' + this.type + "-width: " + item.breakpoint + "px)," + item.breakpoint;
  }, this);
  this.mediaQueries = Array.prototype.filter.call(this.mediaQueries, function (item, index, self) {
    return Array.prototype.indexOf.call(self, item) === index;
  }); // навешивание слушателя на медиа-запрос
  // и вызов обработчика при первом запуске

  var _loop = function _loop(_i2) {
    var media = _this2.mediaQueries[_i2];
    var mediaSplit = String.prototype.split.call(media, ',');
    var matchMedia = window.matchMedia(mediaSplit[0]);
    var mediaBreakpoint = mediaSplit[1]; // массив объектов с подходящим брейкпоинтом

    var оbjectsFilter = Array.prototype.filter.call(_this2.оbjects, function (item) {
      return item.breakpoint === mediaBreakpoint;
    });
    matchMedia.addListener(function () {
      _this.mediaHandler(matchMedia, оbjectsFilter);
    });

    _this2.mediaHandler(matchMedia, оbjectsFilter);
  };

  for (var _i2 = 0; _i2 < this.mediaQueries.length; _i2++) {
    _loop(_i2);
  }
};

DynamicAdapt.prototype.mediaHandler = function (matchMedia, оbjects) {
  if (matchMedia.matches) {
    for (var _i3 = 0; _i3 < оbjects.length; _i3++) {
      var оbject = оbjects[_i3];
      оbject.index = this.indexInParent(оbject.parent, оbject.element);
      this.moveTo(оbject.place, оbject.element, оbject.destination);
    }
  } else {
    for (var _i4 = 0; _i4 < оbjects.length; _i4++) {
      var _оbject = оbjects[_i4];

      if (_оbject.element.classList.contains(this.daClassname)) {
        this.moveBack(_оbject.parent, _оbject.element, _оbject.index);
      }
    }
  }
}; // Функция перемещения


DynamicAdapt.prototype.moveTo = function (place, element, destination) {
  element.classList.add(this.daClassname);

  if (place === 'last' || place >= destination.children.length) {
    destination.insertAdjacentElement('beforeend', element);
    return;
  }

  if (place === 'first') {
    destination.insertAdjacentElement('afterbegin', element);
    return;
  }

  destination.children[place].insertAdjacentElement('beforebegin', element);
}; // Функция возврата


DynamicAdapt.prototype.moveBack = function (parent, element, index) {
  element.classList.remove(this.daClassname);

  if (parent.children[index] !== undefined) {
    parent.children[index].insertAdjacentElement('beforebegin', element);
  } else {
    parent.insertAdjacentElement('beforeend', element);
  }
}; // Функция получения индекса внутри родителя


DynamicAdapt.prototype.indexInParent = function (parent, element) {
  var array = Array.prototype.slice.call(parent.children);
  return Array.prototype.indexOf.call(array, element);
}; // Функция сортировки массива по breakpoint и place
// по возрастанию для this.type = min
// по убыванию для this.type = max


DynamicAdapt.prototype.arraySort = function (arr) {
  if (this.type === "min") {
    Array.prototype.sort.call(arr, function (a, b) {
      if (a.breakpoint === b.breakpoint) {
        if (a.place === b.place) {
          return 0;
        }

        if (a.place === "first" || b.place === "last") {
          return -1;
        }

        if (a.place === "last" || b.place === "first") {
          return 1;
        }

        return a.place - b.place;
      }

      return a.breakpoint - b.breakpoint;
    });
  } else {
    Array.prototype.sort.call(arr, function (a, b) {
      if (a.breakpoint === b.breakpoint) {
        if (a.place === b.place) {
          return 0;
        }

        if (a.place === "first" || b.place === "last") {
          return 1;
        }

        if (a.place === "last" || b.place === "first") {
          return -1;
        }

        return b.place - a.place;
      }

      return b.breakpoint - a.breakpoint;
    });
    return;
  }
};

var da = new DynamicAdapt("max");
da.init();

if (document.querySelector('.product__stock-value')) {
  var STEP = 1;
  var MAX_VALUE;
  var MIN_VALUE = 1;
  var num = document.querySelector('.product__stock-value').innerHTML;
  MAX_VALUE = num;
  var btnPlus = document.querySelector('.plus');
  var btnMinus = document.querySelector('.minus');
  var field = document.querySelector('.product__quantity-value');
  var fieldValue;

  var onBtnMinusPress = function onBtnMinusPress() {
    fieldValue = parseFloat(field.value);

    if (fieldValue > MIN_VALUE) {
      fieldValue -= STEP;
      field.setAttribute('value', fieldValue);
    }
  };

  var onBtnPlusPress = function onBtnPlusPress() {
    fieldValue = parseFloat(field.value);

    if (fieldValue < MAX_VALUE) {
      fieldValue += STEP;
      field.setAttribute('value', fieldValue);
    }
  };

  var toAddCartCount = function toAddCartCount() {
    field.setAttribute('value', MIN_VALUE);
    btnMinus.addEventListener('click', onBtnMinusPress);
    btnPlus.addEventListener('click', onBtnPlusPress);
  };

  toAddCartCount();
}

if (document.querySelector('.product__btn-add-cart')) {
  var addCartBtn = document.querySelector('.product__btn-add-cart');
  var addCartMessage = document.querySelector('.message');
  addCartBtn.addEventListener('click', function () {
    addCartMessage.classList.add('message--active');
  });
}

if (document.querySelector('.product-table__close-btn')) {
  var btnsDeleteProduct = document.querySelectorAll('.product-table__close-btn');
  var cartMessage = document.querySelector('.cart__message');
  var btnCancel = document.querySelector('.message__btn-cancel');
  var cartEmpryMessage = document.querySelector('.message--cart-empty');
  var cartWrap = document.querySelector('.cart__wrap');

  var checkProductCart = function checkProductCart() {
    if (document.querySelectorAll('.product-row').length == 0) {
      console.log(111);
      cartMessage.classList.add('message--active');
      cartEmpryMessage.classList.add('message--active');
      cartWrap.classList.add('cart__wrap--none');
    }
  };

  btnsDeleteProduct.forEach(function (btn, index) {
    btn.addEventListener('click', function (evt) {
      evt.currentTarget.closest('.product-table__row').remove();
      checkProductCart();
    });
  });
  btnCancel.addEventListener('click', function () {
    cartMessage.classList.remove('message--active');
    cartEmpryMessage.classList.remove('message--active');
  });
}

if (document.getElementById('slider-snap')) {
  var snapSlider = document.getElementById('slider-snap');
  noUiSlider.create(snapSlider, {
    range: {
      min: 70,
      max: 120
    },
    start: [70, 120],
    step: 10,
    connect: true
  });
  var snapValues = [document.getElementById('slider-snap-value-lower'), document.getElementById('slider-snap-value-upper')];
  snapSlider.noUiSlider.on('update', function (values, handle) {
    snapValues[handle].innerHTML = '$ ' + values[handle].split('.')[0];
  });
}

if (document.querySelector('.shop__view-btn')) {
  var viewBtns = document.querySelectorAll('.shop__view-btn');
  var viewCatalogs = document.querySelectorAll('.shop__content');
  viewBtns.forEach(function (viewBtn, index) {
    viewBtn.addEventListener('click', function (evt) {
      viewBtns.forEach(function (viewBtn) {
        viewBtn.classList.remove('shop__view-btn--active');
      });
      evt.currentTarget.classList.add('shop__view-btn--active');
      viewCatalogs.forEach(function (viewCatalog) {
        viewCatalog.classList.toggle('shop__content--active');
      });
    });
  });
}

if (document.querySelector('.header__account-sub')) {
  var count = document.querySelector('.header__account-sub--count');
  var addCarts = document.querySelectorAll('.card__link--cart');
  var headerCart = document.querySelector('.header__cart');
  var headerCartMessage = document.querySelector('.header__account-cart-message');

  var _btnClose = document.querySelector('.header__product-btn-close');

  addCarts.forEach(function (btn, index) {
    btn.addEventListener('click', function (evt) {
      evt.currentTarget.classList.add('card__link--active');

      if (count.innerHTML >= 0) {
        var test = count.innerHTML;
        test++;
        count.innerHTML = test;
        headerCart.classList.add('header__cart--active');
        headerCartMessage.classList.add('header__account-cart-message--none');
      }
    });
  });

  var onBtnDeleteProduct = function onBtnDeleteProduct() {
    addCarts.forEach(function (btn, index) {
      btn.classList.remove('card__link--active');
    });

    if (count.innerHTML = 0) {
      var test = count.innerHTML;
      test = 0;
      count.innerHTML = test;
    }

    headerCart.classList.remove('header__cart--active');
    headerCartMessage.classList.remove('header__account-cart-message--none');
  };

  _btnClose.addEventListener('click', onBtnDeleteProduct);
}

$.mask.definitions['9'] = true;
$.mask.definitions['9'] = "[0-9]";
$("#checkout-phone").mask("+7(999)999-99-99");
$("#call-back-phone").mask("+7(999)999-99-99");
$("#center_ok").click(function () {
  $(this).setCursorPosition(3);
}).mask("+7(999) 999-9999");
$("#center_not_ok").mask("+7(999)999-99-99");
var popupLinks = document.querySelectorAll('.popup__link'); // const body = document.querySelector('body');

var popupOpen = function popupOpen(currentPopup) {
  if (currentPopup) {
    var popupActive = document.querySelector('.popup--open');

    if (popupActive) {
      popupClose(popupActive);
    }
  }

  body.classList.add('fixed');
  currentPopup.classList.add('popup--open');
  currentPopup.addEventListener('click', function (e) {
    if (!e.target.closest('.popup__content')) {
      popupClose(e.target.closest('.popup'));
    }
  });
};

var popupClose = function popupClose(currentPopup) {
  currentPopup.classList.remove('popup--open');
  body.classList.remove('fixed');
};

var closeIcons = document.querySelectorAll('.popup__close');
closeIcons.forEach(function (close) {
  close.addEventListener('click', function () {
    popupClose(close.closest('.popup'));
  });
});
document.addEventListener('keydown', function (e) {
  if (e.which === 27) {
    var popupActive = document.querySelector('.popup--open');
    popupClose(popupActive);
  }
}); // if (document.querySelector('.popup')) {

if (popupLinks.length > 0) {
  popupLinks.forEach(function (popupLink, index) {
    popupLink.addEventListener('click', function (e) {
      var currentPopup = document.getElementById('popup-q-view');
      popupOpen(currentPopup);
      e.preventDefault();
    });
  });
} // }


if (document.querySelector('.footer__call-back-btn')) {
  var btnCallBack = document.querySelector('.footer__call-back-btn');
  btnCallBack.addEventListener('click', function () {
    var currentPopup = document.getElementById('popup-call-back');
    popupOpen(currentPopup);
  });
}

if (document.querySelector('.card')) {
  var cardLinks = document.querySelectorAll('.card__link');
  cardLinks.forEach(function (link) {
    link.addEventListener('click', function (e) {
      if (e.target.closest('.card__links-hover') || e.target.closest('.card__links')) {
        e.preventDefault();
      }
    });
  });
}