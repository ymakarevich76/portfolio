'use strict';

var buttons = document.querySelectorAll('button');

if (buttons) {
  for (var i = 0; i < buttons.length; i++) {
    buttons[i].addEventListener('mouseup', function () {
      this.blur();
    });
  }
}

;
var swiper = new Swiper('.reviews-slider', {
  pagination: {
    el: '.swiper-pagination',
    clickable: true
  },
  slidesPerView: 3,
  centeredSlides: true,
  initialSlide: 3,
  loop: true
});
var imgSlider = new Swiper('.image-slider__container', {
  navigation: {
    nextEl: '.swiper-button-next',
    prevEl: '.swiper-button-prev'
  },
  loop: true,
  spaceBetween: 23,
  slidesPerView: 1,
  breakpoints: {
    426: {
      slidesPerView: 2
    },
    768: {
      slidesPerView: 3
    },
    1440: {
      slidesPerView: 4
    }
  }
});
var videoSlider = new Swiper('.video-slider__container', {
  navigation: {
    nextEl: '.swiper-button-next',
    prevEl: '.swiper-button-prev'
  },
  slidesPerView: 1,
  centeredSlides: true,
  loop: true
});
var bigSlider = new Swiper('.slider-big__container', {
  pagination: {
    el: '.swiper-pagination',
    clickable: true,
    renderBullet: function renderBullet(index, className) {
      return '<span class="' + className + '" > ' + (index + 1) + '</span>';
    }
  },
  navigation: {
    nextEl: '.swiper-button-next',
    prevEl: '.swiper-button-prev'
  },
  loop: true,
  slidesPerView: 1,
  initialSlide: 1
});
;
var hamburger = document.querySelector('.header__burger');
var nav = document.querySelector('.header__nav');
var btnClose = document.querySelector('.header__btn-close');
var body = document.querySelector('body');

var openMenu = function openMenu() {
  body.classList.add('fixed');
  nav.classList.add('header__nav--active');
};

var closeMenu = function closeMenu() {
  body.classList.remove('fixed');
  nav.classList.remove('header__nav--active');
};

hamburger.addEventListener('click', function () {
  openMenu();
});
btnClose.addEventListener('click', function () {
  closeMenu();
});
nav.addEventListener('click', function (evt) {
  if (evt.target === nav) {
    closeMenu();
  }
});
;
var $page = $('html, body');
$('a[href*="#"]').on('click', function () {
  $page.animate({
    scrollTop: $($.attr(this, 'href')).offset().top
  }, 400);
  return false;
}); // const anchors = document.querySelectorAll('a[href*="#"]')
// for (let anchor of anchors) {
//   anchor.addEventListener('click', function (e) {
//     e.preventDefault()
//     const blockID = anchor.getAttribute('href').substr(1)
//     document.getElementById(blockID).scrollIntoView({
//       behavior: 'smooth',
//       block: 'start'
//     })
//   })
// }

;